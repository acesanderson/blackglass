[tool.uv.workspace]
members = ["src/blackglass/blackglass-server", "src/blackglass/blackglass-client"]

# Local dev override — un-comment on MacBook if you need editable dbclients:
# [tool.uv.sources]
# dbclients = { path = "/Users/bianders/Brian_Code/dbclients-project", editable = true }
